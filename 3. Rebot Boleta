  int  dreta = (0);
  int a=+1;
  
void setup(){
  size(500,200);
}

void draw(){
  background(255);
  dreta = dreta + a;
  if (dreta > 500) {
   a=-1;
   dreta=dreta+a;
  }
   if (dreta<0){
     a=+1;
     dreta=dreta+a;
  }
  noStroke();
  fill(255,0,0);
  ellipse(dreta,100,50,50);
}
